{
  "_note": "Vercel reads the vercel.json at the REPO ROOT (../vercel.json). This file is reference only.",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
