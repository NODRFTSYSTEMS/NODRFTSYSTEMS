# Local dev environment — Vite automatically loads this file in dev mode.
# These values are only active when running `vite dev` (not in production builds).
# Supabase credentials will be added here when the backend wires up.

VITE_DEMO_MODE=true
